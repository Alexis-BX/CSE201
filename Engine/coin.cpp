#include "coin.h"

Coin::Coin(pair position, pair size, State state) : Collectable(position, size, state)
{
	
}
/**
void Coin::Value(pair size)
{
    if (size.x * size.y <= 2)
	{
		std::cout << "Value of 3" << std::endl;
	}

    if (pair.x * pair.y <= 6)
	{
		std::cout << "Value of 7" << std::endl;
	}
	else
	{
		std::cout << "Value of 13" << std::endl;
	}
}
**/
